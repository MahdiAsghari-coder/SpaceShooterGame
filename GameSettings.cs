using System;

namespace SpaceShooterGame
{
    public static class GameSettings
    {
        // وضعیت موسیقی در زمان بازی ذخیره میکنن
        public static bool IsMusicMuted = false;
        public static bool IsSFXMuted = false;
    }
}